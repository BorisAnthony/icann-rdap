# My Notes

RUST Build commands:

*Target/Debug/*
- `cargo build -p icann-rdap-cli`

*Target/release*
- `cargo build -p icann-rdap-cli --release`

*Install to $PATH (~/.cargo/bin/)*
- `cargo install --path icann-rdap-cli`